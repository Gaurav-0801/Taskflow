services:
  - type: web
    name: taskflow-backend
    env: node
    rootDir: backend
    buildCommand: rm -rf node_modules .npm && npm cache clean --force && npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001

